# Landmark Extraction — CPU-only, lightweight
# Used for: python train.py --extract_landmarks

# Pose Estimation & Computer Vision
# Pin mediapipe to 0.10.14 — last stable version with mp.solutions.holistic API
mediapipe==0.10.14
opencv-python>=4.8.0

# Data Processing
numpy>=1.24.0,<2.0.0
tqdm>=4.65.0
